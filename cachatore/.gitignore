# Cachatore — herramienta Python (sniper de turnos YPF en iTurnos).
# Runtime y CREDENCIALES: NUNCA al repo.

# Credenciales reales de iTurnos.
# claves.json: las claves que carga Santiago (plantilla: claves.ejemplo.json).
# cuentas.json: legacy del esquema manual viejo (hoy los datos salen de Firestore).
claves.json
cuentas.json

# Selección del drop del día (la escribe la UI; plantilla: drop.ejemplo.json).
drop.json
captura_*.html

# Virtualenv (regenerable: python -m venv venv && pip install curl_cffi beautifulsoup4)
venv/
__pycache__/

# Capturas del formulario de reserva (pueden traer datos de sesión).
captura_*.html
