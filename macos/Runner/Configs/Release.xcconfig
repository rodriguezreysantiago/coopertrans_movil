// CocoaPods (#include? = opcional; el archivo aparece después de
// pod install y no está versionado).
#include? "../../Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "../../Flutter/Flutter-Release.xcconfig"
#include "Warnings.xcconfig"

// NOTA: los parches STRIP_INSTALLED_PRODUCT=NO y OTHER_CODE_SIGN_FLAGS=--deep
// que estuvieron acá (commits 74b4402 + 2a21fe6, Builds 40/43) NO resolvieron
// el "code object is not signed at all" del .app raíz. Reemplazados por un
// Run Script Phase en el target Runner del pbxproj que firma manualmente el
// binario interno antes del CodeSign implícito del .app (ver el shell phase
// "CodeSign binary interno (workaround Xcode Cloud macOS)" en project.pbxproj).
